    <!-- Sidebar -->
    <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <%= link_to  "Home", user_path(current_user) %>
                </li>
                <li class="avatar">
                    <%= image_tag @user.image.url, class: 'avatar-pic' %>
                    <br>
                    Welcome <%= current_user.f_name %>
                </li>
                <li class="dropdown">
                    <a class="dropdown-toggle" id="dropdownMenu" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Blogs
                    </a>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenu">
                        <%= link_to  "View All Blogs", categories_path %>
                        <div class="dropdown-divider"></div>
                        <%= link_to  "Personal Blogs", user_blogs_path(current_user.id) %>
                        <%= link_to  "Create Blog", new_user_blog_path(current_user.id) %>
                        <%= link_to  "Comments Made", user_comments_path(current_user.id) %>
                    </div>
                </li>
                <li>
                    <%= link_to  "About", about_path %>
                </li>
                <li>
                    <%= link_to  "User Settings", edit_user_path(current_user.id) %>
                </li>
                <li class="dropdown">
                        <a class="dropdown-toggle" id="dropdownMenu" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Search
                        </a>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenu">
                        
                        </div>
                    </li>
            </ul>
        </div>